Fields needed for DOTs:

✔ word guessing ➡ input
✔ word generated ⬅ output
✔ result word ⬅ output
✔ attempts and credits
✔  credits loop
✔  new word loaded when failed word with + credits

name ➡ input
location ⬅ via API output : not visible, just for database stats

tries ↔ dynamic
attempts ↔ dynamic
credits ↔ dynamic

Later: is there a pattern I can use for all the conditionals?

Example game:
https://www.vertabelo.com/blog/tutorial-how-to-develop-a-simple-tic-tac-toe-game-with-spring-boot-and-angularjs/

Validation fixed:
added dependency extra:
            <dependency>
                <groupId>javax.validation</groupId>
                <artifactId>validation-api</artifactId>
            </dependency>


For Blog:

Spring MVC: Neither BindingResult nor plain target object for bean name 'OBJECTNAME' available as request attribute.

Solution: @ModelAttribute("OBJECTNAME")
          public OBJECTNAME nameOfMethodDoesntMatter(){
              return new OBJECTNAME();
          }